apiVersion: v1
kind: Service
metadata:
  name: {{ include "wp-fleet-ops.fullname" . }}
  labels:
    {{- include "wp-fleet-ops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wp-fleet-ops.selectorLabels" . | nindent 4 }}
