{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "replicaCount": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1,
      "description": "WP FleetOps requires exactly one replica because it uses a single SQLite database."
    },
    "progressDeadlineSeconds": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum time in seconds for a Deployment rollout to make progress before Kubernetes reports failure."
    },
    "deploymentStrategy": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": ["Recreate"],
          "description": "WP FleetOps requires non-overlapping Pod replacement because it uses SQLite on a shared RWO volume."
        }
      }
    }
  }
}